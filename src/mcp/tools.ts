// Backward-compatibility shim — re-exports from the canonical location.
// The actual implementation lives in src/transports/mcp/handlers.ts.
export { handleTool, TOOL_DEFINITIONS } from '../transports/mcp/handlers.js';
